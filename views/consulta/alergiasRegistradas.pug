h4.mt-4 Alergias Registradas
table.table.table-sm(id="tablaAlergias")
  thead
    tr 
      th(scope='col') Nombre
      th(scope='col') Fecha desde 
      th(scope='col') Importancia
      th(scope='col') Acciones
  tbody                       

h5.mt-4 Agregar una nueva alergia
.row
  .col 
    //- Cambié el input de texto a un select para las alergias
    select.form-select(id='selectNombreAlergia' aria-label='Nombre de Alergia')
      option(value='' selected) Seleccione una alergia
      each item in alergiasJson
        option(value=item.nombreCodigo) #{item.nombreCodigo}
  .col
    input.form-control(type="date" name="fecha" id="fechaAlergia" value=fechaSeleccionada)
  .col
    select.form-select(id='selectImportancia' aria-label='Importancia')
      option(value='' selected) Importancia
      option(value='Leve') Leve
      option(value='Normal') Normal
      option(value='Alta') Alta

button.btn.btn-primary.mt-4(type='button' onClick="agregarFilaAlergia()") Agregar alergia
hr
