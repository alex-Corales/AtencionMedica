h4.mt-4 Alergias Registradas
table.table.table-sm(id="tablaAlergias")
  thead
    tr 
      th(scope='col') Nombre
      th(scope='col') Fecha desde 
      th(scope='col') Importancia
  each alergia in alergias                
    tbody 
      tr
        td #{alergia.nombre}
        td #{alergia.fecha_desde.toISOString().split('T')[0]}
        td #{alergia.importancia}
h5.mt-4 Agregar una nueva alergia
.row
  .col 
    input.form-control(type='text' id='inputNombreAlergia' placeholder='Ingrese el nombre' aria-label='Nombre')
  .col
    input.form-control(type="date" name="fecha" id="fechaAlergia" value=fechaSeleccionada)
  .col
    select.form-select(id='selectImportancia' aria-label='Importancia')
      option(value='' selected) Importacia
      option(value='Baja') Baja
      option(value='Media') Media
      option(value='Alta') Alta

button.btn.btn-secondary.mt-4(type='button' onClick="agregarFilaAlergia()") Agregar alergia
hr
